  <div class="left">

    <%= link_to "Nouveau", new_cake_path %>

    <div class="products">
      <% @cakes.each do |cake| %>

        <%= render 'card', cake: cake %>

      <% end %>
    </div>
  </div>
