
  <% if current_user.addresses.exists? %>
    <div class="label"><%= t('activerecord.attributes.address.phone') %></div>
    <p class="address-field"><%= format(@address.phone) %><p>
  <% end %>
  <div class="label"><%= t('activerecord.attributes.user.email') %></div>
    <p class="address-field"><%= @basket.user.email %><p>
  <div class="actions">
    <%= link_to t('link.edit_email'), edit_user_registration_path(@basket.user), class: "action" %>
  </div>
