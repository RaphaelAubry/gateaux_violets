  <p><%= @basket.user.email %><p>
  <%= link_to "", edit_user_registration_path(@basket.user), class: "fas fa-edit" %>
  <% if current_user.addresses.exists? %>
    <p><%= format(@address.phone) %><p>
  <% end %>
